import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#1b325f",
      light: "#485B7F",
      dark: "#122342",
    },
    secondary: {
      main: "#5095be",
      light: "#73AACB",
      dark: "#386885",
    },
  },
});
